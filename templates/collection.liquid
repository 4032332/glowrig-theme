<div class="collection-page">
  <div class="container">
    <div class="collection-hero">
      <span class="section-eyebrow">{{ collection.title }}</span>
      <h1 class="section-title">{{ collection.title }}</h1>
      {% if collection.description != blank %}<p class="section-desc">{{ collection.description }}</p>{% endif %}
    </div>
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:32px;flex-wrap:wrap;gap:16px;">
      <div class="collection-filters">
        <button class="filter-btn active">All</button>
        {% if collection.handle == 'desk-mats' %}
          <button class="filter-btn">Cosmic Series</button>
          <button class="filter-btn">Japanese Series</button>
        {% elsif collection.handle == 'lighting' %}
          <button class="filter-btn">Light Bars</button>
          <button class="filter-btn">Bias Lighting</button>
          <button class="filter-btn">Ambient</button>
        {% elsif collection.handle == 'organisation' %}
          <button class="filter-btn">Headphone Stands</button>
          <button class="filter-btn">Cable Management</button>
        {% else %}
          <button class="filter-btn">New Arrivals</button>
          <button class="filter-btn">Best Sellers</button>
        {% endif %}
      </div>
      <span style="font-size:13px;color:var(--text-muted);">{{ collection.products_count }} products</span>
    </div>

    {% paginate collection.products by 24 %}
      {% if collection.products.size > 0 %}
        <div class="products-grid stagger">
          {% for product in collection.products %}
            {% render 'product-card', product: product %}
          {% endfor %}
        </div>

        {% if paginate.pages > 1 %}
          <div style="display:flex;justify-content:center;gap:8px;margin-top:48px;flex-wrap:wrap;">
            {% if paginate.previous %}
              <a href="{{ paginate.previous.url }}" class="btn btn-outline" style="padding:10px 20px;">← Previous</a>
            {% endif %}
            {% for part in paginate.parts %}
              {% if part.is_link %}
                <a href="{{ part.url }}" class="btn btn-outline" style="padding:10px 16px;">{{ part.title }}</a>
              {% else %}
                <span class="btn btn-primary" style="padding:10px 16px;cursor:default;">{{ part.title }}</span>
              {% endif %}
            {% endfor %}
            {% if paginate.next %}
              <a href="{{ paginate.next.url }}" class="btn btn-outline" style="padding:10px 20px;">Next →</a>
            {% endif %}
          </div>
        {% endif %}
      {% else %}
        <div style="text-align:center;padding:96px 0;">
          <div style="font-size:64px;margin-bottom:24px;">🎮</div>
          <h2 style="font-family:var(--font-heading);font-size:28px;margin-bottom:12px;">Products incoming</h2>
          <p style="color:var(--text-secondary);margin-bottom:32px;">This collection is being stocked. Check back soon.</p>
          <a href="{{ routes.all_products_collection_url }}" class="btn btn-primary">Shop All Products</a>
        </div>
      {% endif %}
    {% endpaginate %}
  </div>
</div>
